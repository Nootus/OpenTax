/**
 * Capital Gains Foreign Summary Model
 * 
 * Summary model for foreign capital gains display.
 * Data comes from capitalForeign widget (ForeignAssetsWidget)
 */

export interface CapitalGainsForeignSummary {
  filingId?: number
  foreignCount: number
  foreignTotal: number
}
