/**
 * Capital Gains Movable Summary Model
 * 
 * Summary model for movable assets capital gains display.
 * Data comes from capitalMovable widget (MovableAssetsWidget)
 */

export interface CapitalGainsMovableSummary {
  filingId?: number
  movableCount: number
  movableTotal: number
}
