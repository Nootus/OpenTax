# OpenTax API

> This directory will contain the backend API for OpenTax.
> Not implemented yet — see `docs/08-PHASE-7-SUBMIT.md` for planned approach.

## Planned Endpoints

- `POST /api/filing` — Accept a complete `FilingModel` JSON and store it
